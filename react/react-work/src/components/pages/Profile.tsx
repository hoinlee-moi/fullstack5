import UserProfile from '../organisms/UserProfile';

const Profile = () => {
  return (
    <div className='flex flex-col justify-center items-center min-h-[calc(100vh-3.5rem)]'>
      <UserProfile />
    </div>
  );
};

export default Profile;
